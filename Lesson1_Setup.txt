- Mac setup for ssh to remote server:

    ssh root@172.105.25.135 - here root is the username and after @ is the IP address of the server
    input password

- Windows setup for ssh to remote server:

    Install Putty
    Input IP address of the server
    Leave port as 22
    then enter username and password as in case of mac

- Update software on the system (not necessary but preferred)

    sudo apt update
    sudo apt upgrade